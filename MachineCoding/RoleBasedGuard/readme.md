Implement a role-based route guarding system using React Router. Some routes should be public while others require authentication and specific roles (e.g., admin or user). 

Unauthorized users should be redirected appropriately, and loading states should be handled during auth checks. Example: A non-admin user attempting to access /admin is redirected to /login or /unauthorized.